{
  "name": "gleameet",
  "version": "1.0.0",
  "private": true,
  "description": "Private Real-Time AI Meeting Coach",
  "workspaces": [
    "packages/shared",
    "packages/law-registry",
    "packages/backend",
    "packages/extension",
    "packages/teams-app",
    "packages/zoom-app"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:shared": "npm run build -w packages/shared",
    "build:law-registry": "npm run build -w packages/law-registry",
    "build:backend": "npm run build -w packages/backend",
    "build:extension": "npm run build -w packages/extension",
    "dev:backend": "npm run dev -w packages/backend",
    "dev:extension": "npm run dev -w packages/extension",
    "db:init": "psql -d gleameet -f packages/backend/src/db/schema.sql",
    "docker:infra": "docker-compose up -d postgres redis",
    "lint": "eslint packages/*/src/**/*.ts",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
